- Stabilized Kaggle persistence model: updated quickstart.sh and rebuild.sh to use persistent runtime storage at /kaggle/working/.openclaw/root-openclaw-live with /root/.openclaw symlinked there; added compact backup snapshot of /root/.openclaw to /kaggle/working/.openclaw/root-openclaw-backup and safer autosave/autopush behavior.
- Fixed quickstart false-negative gateway startup behavior by changing gateway health check to retry/warm up before failing; gateway was actually coming up even when script reported failure.
- Installed and repaired n8n on Kaggle: global n8n binary path issues fixed via /usr/local/bin/n8n symlink to /usr/lib/node_modules/n8n/bin/n8n, plus sqlite3 added inside n8n install so it can start.
- n8n now starts successfully on port 5678 when launched with public URL env vars; quickstart/rebuild now support Kaggle secret N8N_PUBLIC_URL and export N8N_EDITOR_BASE_URL / WEBHOOK_URL / N8N_PROTOCOL / N8N_HOST / N8N_PORT accordingly.
- Built Phase 1 Google Drive/Sheets helper at scripts/factory_drive_sync.py: bootstraps per-job Drive folders, local metadata, Sheets rows, supports human-readable folder naming (persona_name - job_id) with subfolders intake/references/dataset/previews/approvals/lora/final_batches/delivery/logs/metadata.
- Verified service-account integration can create Drive folders and update Google Sheets, but direct file upload to Drive fails with HTTP 403 "Service Accounts do not have storage quota". Conclusion: service account is okay for folder bootstrap + Sheets, but not for file uploads in current Drive setup.
- Built n8n handoff for uploads: created n8n/factory_preview_upload_workflow.json and docs/N8N_PREVIEW_UPLOAD.md; Kaggle helper can generate upload payload JSON for n8n via prepare-n8n-upload.
- Verified n8n has credentials named "Google Drive account" and "Google Sheets account", but API test showed both are currently not fully connected to a Google account (error: "This OAuth2 credential was not connected to an account.").
- Diagnosed n8n OAuth issues: first callback was going to localhost due to missing public URL config; after fixing that, current blocker is OAuth Authorization Error with HTTP 414 (URI Too Long) through use.devtunnels.ms. Recommendation given: switch n8n to a different public tunnel (prefer Cloudflare Tunnel or ngrok) instead of continuing with current dev tunnel.
- Created missing memory/ directories with .gitkeep for all agent workspaces (main, manager, maya, jordan, sam, creative-lab, prompt-engineer, ops-guardian, watchdog, validator, recovery, n8n-worker). Main HEARTBEAT.md exists in /kaggle/working/.openclaw/workspace/HEARTBEAT.md.
- User preference captured in USER.md: do as much execution directly as possible and only ask the user to intervene when it is genuinely impossible or blocked for the agent.
- Fixed persistent n8n/OpenClaw startup on Kaggle: N8N_USER_FOLDER now points at persistent storage, quickstart no longer overwrites live OpenClaw config with stale backup, gateway starts last, and startup no longer hard-fails just because gateway health checks are noisy.
- n8n Google Drive and Google Sheets user OAuth are now working and persisting across restarts; Drive upload/download was verified and n8n webhook-based Drive uploads are working in production on this Kaggle box.
- Replaced the preview upload path with a working active n8n webhook (`factory-preview-upload-v2`) that accepts batch/base64 uploads and returns Drive links; preview_upload.py now updates job.json correctly and advances jobs to preview_review.
- Built the factory job orchestration layer: schemas/job_schema.json plus scripts/job_manager.py, preview_upload.py, approval_handler.py, training_handler.py, final_batch_handler.py, and delivery_handler.py.
- Proven full end-to-end factory pipeline on test jobs: create job -> bootstrap Drive/Sheets -> upload previews -> approve -> start/complete training (simulated) -> upload finals -> QA approve -> deliver. `test-001` and `REAL-001` both reached final status `delivered` with Drive links recorded in job.json.
- Added persistence/ops layer for the factory: scripts/factory_backup.sh, factory_restore.sh, factory_sync_to_drive.py, factory_boot.sh, run_job.sh, and migrate_old_jobs.py. Backup/restore/state export/boot checks all ran successfully.
- Modal ComfyUI generation is now integrated and proven: `generate_previews.py` and `generate_finals.py` generate real images through the Modal endpoint, detect actual returned file type, and save with correct `.png` extension. Cold start observed ~70s; warm generations ~20s/image.
- Prompt generation is now wired through OpenRouter via scripts/prompt_generator.py with Kaggle-secrets fallback for OPENROUTER_API_KEY, retry logic, and uncensored-friendly fallback models. Primary dolphin-mistral free model is often rate-limited; working fallback used successfully was `nvidia/nemotron-3-super-120b-a12b:free`.
- Proven the upstream creative loop on `PROMPT-TEST-001`: AI-generated prompts saved to intake, generate_previews.py loaded those saved prompts, generated preview images from them, uploaded previews to Drive, and advanced the job to preview_review.
- `JOB-20260312-001` was migrated from the old flat job metadata shape into the newer nested job format so job_manager.py list/export/backup behave more consistently.
